{
  "name": "cronify",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf dist && rimraf out && rimraf ui && rimraf .*.bun-build",
    "dev-build": "bun clean && cd frontend && bun install && bun run build && cd .. && bun install && bun build ./index.ts --compile --outfile ./dist/cronify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^6.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cron-parser": "^5.5.0"
  }
}

{
  "name": "cronify",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf dist && rimraf out && rimraf ./cronify",
    "dev": "bun index.ts daemon --start",
    "dev-build": "bun build ./index.ts --compile --outfile cronify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^6.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cron-parser": "^5.5.0"
  }
}

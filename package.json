{
  "name": "cronify",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "bun -e \"if (process.platform === 'win32') { require('child_process').execSync('rimraf dist out') } else { require('child_process').execSync('rimraf dist out .bun-build*') }\"",
    "dev-build": "bun build ./index.ts --compile --outfile ./dist/cronify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^6.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cron-parser": "^5.5.0"
  }
}

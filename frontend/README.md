# Frontend (Web UI/Dashboard)

- React
- Vite
- Tailwind CSS

The frontend is bundled into a single `index.html` file using `vite-plugin-singlefile` so it can be embedded directly into the Cronify binary.

name: build-cronify

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          # =====================
          # LINUX
          # =====================
          - os: ubuntu-24.04
            artifact_name: cronify-linux-x64
            binary_name: cronify
            archive_name: cronify-${{ github.ref_name }}-linux-x64.zip

          - os: ubuntu-24.04-arm
            artifact_name: cronify-linux-arm64
            binary_name: cronify
            archive_name: cronify-${{ github.ref_name }}-linux-arm64.zip

          # =====================
          # MACOS
          # =====================
          - os: macos-15
            artifact_name: cronify-macos-arm64
            binary_name: cronify
            archive_name: cronify-${{ github.ref_name }}-macos-arm64.zip

          - os: macos-15-intel
            artifact_name: cronify-macos-x64
            binary_name: cronify
            archive_name: cronify-${{ github.ref_name }}-macos-x64.zip

          # =====================
          # WINDOWS
          # =====================
          - os: windows-2025
            artifact_name: cronify-windows-x64
            binary_name: cronify.exe
            archive_name: cronify-${{ github.ref_name }}-windows-x64.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build frontend
        working-directory: frontend
        run: |
          bun install
          bun run build

      - name: Build binary
        run: bun build ./index.ts --compile --outfile ./dist/${{ matrix.binary_name }}

      # =====================
      # ZIP (Unix)
      # =====================
      - name: Create zip archive (Unix)
        if: runner.os != 'Windows'
        run: |
          cd dist
          zip ${{ matrix.archive_name }} ${{ matrix.binary_name }}

      # =====================
      # ZIP (Windows)
      # =====================
      - name: Create zip archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive `
            -Path dist/${{ matrix.binary_name }} `
            -DestinationPath dist/${{ matrix.archive_name }}

      - name: Upload artifact for release job
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.archive_name }}

  release:
    needs: build
    runs-on: ubuntu-24.04

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find dist -type f -name "*.zip" -exec mv {} release/ \;

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.zip
          generate_release_notes: true
